const UserDataClient = require("../clients/user.dataclient")

class TopThreeThingsLinkageGenerator {
    /**
     * @param {UserDataClient} userDataClient 
     */
    constructor(userDataClient, ctx) {
        this.userDataClient = userDataClient
        this.fhirStore = fhirStore
    }

    async generateLinkages(userId) {
        const 
    }
}
